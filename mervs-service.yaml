apiVersion: v1
kind: Service
metadata:
  name: mervs-demo-app-harbor-svc
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: mervs-demo-app
  ports:
    - port: 3000
      targetPort: 8080
